#include "ShrapnelShell.h"

	// Shrapnel Shell

ShrapnelShell::ShrapnelShell(b2World* world, Config* config, b2Vec2 position, float angle) : Shell(world, config, position, angle)
{

}


ShrapnelShell::~ShrapnelShell()
{
}
